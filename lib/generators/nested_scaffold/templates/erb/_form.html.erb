<%%= form_for [@<%= pr.singular_table_name %>, @<%= r.singular_table_name %>] do |f| %>
  <%% if @<%= r.singular_table_name %>.errors.any? %>
    <div id="error_explanation">
      <h2><%%= pluralize(@<%= r.singular_table_name %>.errors.count, "error") %> prohibited this <%= r.singular_table_name %> from being saved:</h2>

      <ul>
      <%% @<%= r.singular_table_name %>.errors.full_messages.each do |msg| %>
        <li><%%= msg %></li>
      <%% end %>
      </ul>
    </div>
  <%% end %>

  <fieldset>
		<legend><%= r.class_name %></legend>
		<ul>
<% attributes.each do |attribute| -%>
			<li>
		    <%%= f.label :<%= attribute.name %> %><br />
		    <%%= f.<%= attribute.field_type %> :<%= attribute.name %> %>
			</li>
<% end -%>
		</ul>
  </fieldset>
  <div class="actions">
    <%%= f.submit %>
  </div>
<%% end %>